/**
 * todoSlice.js
 *
 * Placeholder slice per data-model.md §todoSlice.
 * Full wiring (personal + professional TODOs with assignment) lands in Phase 3.
 *
 * Owns: items
 */

export const todoSliceInitial = {
  items: [],  // TodoItem[] — Phase 3 adds full shape
};

/**
 * Placeholder — no actions yet. Actions will land in Phase 3.
 *
 * @returns {Object} empty actions object
 */
export function createTodoSlice() {
  return {};
}
